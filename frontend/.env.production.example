# Production build: point API to backend behind Front Door
# Use for: npm run build (or set in CI/CD)
# Do not commit real secrets; use pipeline variables or Key Vault.
REACT_APP_API_URL=https://students-api.hasc.net/api
